---
'@doxajs/praxis': patch
---

Migrate pre-alpha.7 scaffolds from `src/application.ts` to the canonical `app.config.ts` root during
`doxa upgrade`, retire generated application-owned framework routes and providers from composition,
and align the TypeScript project before validation.
