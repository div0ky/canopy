# `@canopy/twilio-sms`

The first-party Twilio implementation of Canopy's provider-independent SMS contracts, including
Messaging Service delivery, callback verification, state normalization, and opt-out classification.

```sh
pnpm add @canopy/twilio-sms
```
