# `@canopy/sendgrid`

The first-party SendGrid implementation of Canopy's provider-independent mail contracts, including
request translation, delivery normalization, and signed webhook verification.

```sh
pnpm add @canopy/sendgrid
```
